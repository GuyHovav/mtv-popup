{
  "name": "mtv-popup",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm run dev -w client\" \"npm run dev -w server\""
  },
  "dependencies": {
    "@google/genai": "^1.0.0",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
